# =====================================================================
# Lumina — backend dependencies (free-tier stack only)
# =====================================================================
# This app is designed to run on free hosting (Render, Railway, Fly.io
# free tier, etc.) without any GPU or paid SaaS. Every dependency here
# is CPU-only and pulls only the packages needed at runtime.
# =====================================================================

# --- LLM: Google Gemini 2.0 Flash (free tier) ---
google-generativeai>=0.7.2
tenacity>=8.2.0

# --- Local embeddings: FastEmbed BGE-large-en-v1.5 (~400 MB CPU) ---
fastembed>=0.3.0

# --- Local reranker: FlashRank MiniLM (~30 MB CPU) ---
flashrank>=0.2.0

# --- LangChain & LangGraph (CRAG State Graph & Splitters) ---
langchain-text-splitters>=0.3.0
langgraph>=0.0.20

# --- OpenAI SDK (Used for NVIDIA NIM & OpenAI compatible endpoints) ---
openai>=1.12.0

# --- Vector store client (Qdrant; uses external Qdrant Cloud free tier) ---
qdrant-client>=1.10.0

# --- Optional: Supabase metadata (free tier) ---
# Required ONLY if you set SUPABASE_URL + SUPABASE_SERVICE_KEY. Without
# those env vars, SupabaseService degrades gracefully to local mode.
supabase>=2.4.0

# --- Optional: Groq Whisper for audio transcription ---
# Only needed for audio/video ingestion. Free tier.
groq>=0.9.0

# --- Document parsing (pure-CPU, no AI models bundled) ---
# Docling was dropped — it ships ~1.5 GB of AI models unsuitable for free hosting.
# Adaptive parser now uses PyMuPDF directly for PDFs.
pymupdf>=1.24.0
pdfplumber>=0.11.0
python-docx>=1.1.0
python-pptx>=1.0.0

# --- Web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
httpx>=0.27.0

# --- MCP server (used by .opencode / Claude Code integration) ---
mcp>=1.2.0

# --- Web Search Skill ---
tavily-python>=0.5.0

# --- Observability & Distributed Tracing ---
langsmith>=0.1.0

# --- Tests ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
